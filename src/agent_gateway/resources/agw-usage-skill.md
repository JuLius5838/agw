---
name: agw-usage
description: >-
  Display Claude Code and ChatGPT/Codex subscription limits together. Use when
  the user invokes /agw-usage or asks to see unified AGW usage and reset times.
disable-model-invocation: true
---

# Agent Gateway usage

This command is handled locally by the Agent Gateway companion plugin before it
reaches a model. If you can read these instructions, the local plugin is not
active. Tell the user to restart Claude Code and run `agw setup` if the problem
continues. Do not run another tool or estimate usage values.
