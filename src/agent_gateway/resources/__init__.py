"""Packaged, credential-free runtime resources (default model registry, shell snippets).

Loaded at runtime via :mod:`importlib.resources` so they resolve identically for an
editable checkout and a ``uv tool install`` of the built wheel.
"""
